# Jack's AI - Python Dependencies for Render.com
# This file lists all the Python packages required to run the application

# Flask web framework and extensions
Flask==3.0.0              # Core web framework for building the application
Flask-Cors==4.0.0         # Cross-Origin Resource Sharing support for API calls
Werkzeug==3.0.1           # WSGI utilities for Flask

# Google Generative AI (Gemini)
google-generativeai==0.3.2   # Official Google Gemini API client library

# Additional dependencies that google-generativeai requires
google-ai-generativelanguage==0.4.0   # Google AI language models support
google-api-core==2.15.0               # Core Google API client library
google-auth==2.25.2                   # Google authentication library
googleapis-common-protos==1.62.0      # Common protocol buffers
protobuf==4.25.1                      # Protocol buffers for data serialization

# Production server (recommended for Render)
gunicorn==21.2.0          # Production-grade WSGI server for Python apps

# Optional but recommended for better performance
python-dotenv==1.0.0      # Load environment variables from .env file
requests==2.31.0          # HTTP library (used by google-generativeai)
certifi==2023.11.17       # SSL certificate verification
charset-normalizer==3.3.2 # Character encoding detection
idna==3.6                 # International Domain Names support
urllib3==2.1.0            # HTTP client library

# File handling utilities
python-magic==0.4.27      # File type detection (optional, for better file handling)
Pillow==10.1.0           # Image processing library (optional, for image handling)

# Async support (if needed in future)
aiohttp==3.9.1           # Async HTTP client/server
aiofiles==23.2.1         # Async file operations

# Security and validation
itsdangerous==2.1.2      # Secure data serialization for Flask sessions
Jinja2==3.1.2            # Template engine for Flask
MarkupSafe==2.1.3        # HTML/XML markup safe string handling

# Date and time handling
python-dateutil==2.8.2   # Powerful extensions to datetime
pytz==2023.3             # Timezone definitions

# JSON handling improvements
orjson==3.9.10           # Fast JSON library (optional, for better performance)